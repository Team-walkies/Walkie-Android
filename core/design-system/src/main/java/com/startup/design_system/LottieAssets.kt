package com.startup.design_system

/**
 * 애니메이션에 사용되는 Lottie 파일명 상수
 */
object LottieAssets {
    /**
     * 색깔 있는 폭죽 애니메이션 (목표 달성 시)
     */
    const val CONFETTI_COLORED = "walkie_ConfettiColored.json"

    /**
     * 흑백 폭죽 애니메이션 (알 부화 시)
     */
    const val CONFETTI = "walkie_Confetti.json"

    /**
     * 일반 등급 알 깨지는 애니메이션 (파란색)
     */
    const val EGG_NORMAL = "walkie_EggBlue.json"

    /**
     * 희귀 등급 알 깨지는 애니메이션 (초록색)
     */
    const val EGG_RARE = "walkie_EggGreen.json"

    /**
     * 에픽 등급 알 깨지는 애니메이션 (노란색)
     */
    const val EGG_EPIC = "walkie_EggYellow.json"

    /**
     * 전설 등급 알 깨지는 애니메이션 (보라색)
     */
    const val EGG_LEGEND = "walkie_EggPurple.json"

    /**
     * 헬스케어 안내 애니메이션
     */
    const val HEALTHCARE_INTRO = "walkie_healthcare1_1.json.json"
}
